[package]
name = "mkbroadcast"
version = "0.0.1"
edition = "2021"

[profile.release]
panic = 'abort'
strip = "symbols"

[dependencies]
pnet = "0.28.0"
shiplift = { version = "0.7", default-features = false, features = ["unix-socket"] }
tokio = { version = "1.15.0", features = ["full"] }

[dev-dependencies]
tokio-test = "*"