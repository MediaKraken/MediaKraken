[package]
name = "theater_thin"
version = "0.0.1"
edition = "2024"

[profile.release]
panic = 'abort'
strip = "symbols"
lto = true
codegen-units = 1

[dependencies]
mk_lib_network = { version="0.0.15", registry = "kellnr" }

fltk = { version = "1.5.4", features = ["fltk-bundled"] }
reqwest = { version = "0.12.14", default-features = false, features = [
    "json",
    "rustls-tls",
] }
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.140"
stdext = "0.3.3"

[dev-dependencies]
tokio-test = "*"
