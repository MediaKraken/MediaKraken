[package]
name = "pi_upc"
version = "0.0.1"
edition = "2021"

[profile.release]
panic = 'abort'
strip = "symbols"
lto = true
codegen-units = 1

[dependencies]
fltk = { version = "1.4.34", features = ["fltk-bundled"] }
sqlite = "0.36.1"

[dev-dependencies]
tokio-test = "*"
