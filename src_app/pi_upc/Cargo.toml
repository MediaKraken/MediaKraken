[package]
name = "pi_upc"
version = "0.0.1"
edition = "2024"

[profile.release]
panic = 'abort'
strip = "symbols"
lto = true
codegen-units = 1

[dependencies]
fltk = { version = "1.5.8", features = ["fltk-bundled"] }
sqlite = "0.37.0"

[dev-dependencies]
tokio-test = "*"
