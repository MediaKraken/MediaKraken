apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: mkstack-elk
  name: mkstack-elk
spec:
  ports:
    - name: "5601"
      port: 5601
      targetPort: 5601
  selector:
    io.kompose.service: mkstack-elk
