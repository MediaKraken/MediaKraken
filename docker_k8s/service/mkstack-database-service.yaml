apiVersion: v1
kind: Service
metadata:
  name: mkstack-database-service
  namespace: mediakraken
spec:
  ports:
    - name: "5432"
      port: 5432
      targetPort: 5432
  selector:
    io.kompose.service: mkstack-database-service
