apiVersion: v1
kind: Service
metadata:
  name: mkstack-portainer-service
  namespace: mediakraken
spec:
  type: NodePort
  ports:
    - name: "9000"
      port: 9000
      targetPort: 9000
      nodePort: 30900      
  selector:
    io.kompose.service: mkstack-portainer-service
