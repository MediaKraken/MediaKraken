apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: mkstack-webapp-service
  name: mkstack-webapp-service
  namespace: mediakraken
spec:
  ports:
    - name: "8900"
      port: 8900
      targetPort: 8080
  selector:
    io.kompose.service: mkstack-webapp-service
