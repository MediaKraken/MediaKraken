apiVersion: v1
kind: Service
metadata:
  labels:
    io.kompose.service: mkstack-pgadmin-service
  name: mkstack-pgadmin-service
  namespace: mediakraken
spec:
  ports:
    - name: "5050"
      port: 5050
      targetPort: 5050
  selector:
    io.kompose.service: mkstack-pgadmin-service
