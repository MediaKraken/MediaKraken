apiVersion: v1
kind: Service
metadata:
  name: mkstack-pgadmin-service
  namespace: mediakraken
spec:
  type: NodePort
  ports:
    - name: "5050"
      port: 5050
      targetPort: 5050
      nodePort: 35050
  selector:
    io.kompose.service: mkstack-pgadmin-service
