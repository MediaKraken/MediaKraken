apiVersion: v1
kind: Service
metadata:
  name: mkstack-grafana-service
  namespace: mediakraken
spec:
  type: NodePort
  ports:
    - name: "3000"
      port: 3000
      targetPort: 3000
      nodePort: 33000
  selector:
    io.kompose.service: mkstack-grafana-service
