apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    io.kompose.service: mkstack-schedulesdirectupdate
  name: mkstack-schedulesdirectupdate
  namespace: mediakraken
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: mkstack-schedulesdirectupdate-service
  template:
    metadata:
      labels:
        io.kompose.service: mkstack-schedulesdirectupdate-service
    spec:
      containers:
        - args:
            - sh
            - -c
            - /wait && /mkschedulesdirectupdate
          env:
            - name: WAIT_HOSTS
              value: mkstack-database:5432, mkstack-rabbitmq:5672
            - name: WAIT_TIMEOUT
              value: "180"
          image: mediakraken/mkschedulesdirectupdate:dev
          name: mkstack-schedulesdirectupdate
          volumeMounts:
            - mountPath: /run/secrets
              name: db-password
              subPath: db-password
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
      volumes:
        - name: db-password
          secret:
            items:
              - key: db-password
                path: db-password
            secretName: db-password
