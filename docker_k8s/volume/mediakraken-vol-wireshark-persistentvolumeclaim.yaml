apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: mediakraken-vol-wireshark
  name: mediakraken-vol-wireshark
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
