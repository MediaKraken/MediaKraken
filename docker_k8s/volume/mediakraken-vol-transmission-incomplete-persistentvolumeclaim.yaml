apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: mediakraken-vol-transmission-incomplete
  name: mediakraken-vol-transmission-incomplete
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
