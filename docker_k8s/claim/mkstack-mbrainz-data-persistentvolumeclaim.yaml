apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mkstack-mbrainz-claim
  namespace: mediakraken
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  storageClassName: nfs-csi
