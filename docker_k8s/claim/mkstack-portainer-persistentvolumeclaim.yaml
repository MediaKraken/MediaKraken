apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mkstack-portainer-claim
  namespace: portainer
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
  storageClassName: nfs-csi
