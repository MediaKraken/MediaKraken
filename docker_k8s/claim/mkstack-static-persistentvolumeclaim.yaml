apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mkstack-static-claim
  namespace: mediakraken
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 250Gi
  storageClassName: nfs-csi
