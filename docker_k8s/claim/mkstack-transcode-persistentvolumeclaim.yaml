apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mkstack-transcode-claim
  namespace: mediakraken
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-csi
