apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: mediakraken-vol-mumble
  name: mediakraken-vol-mumble
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
