apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: mediakraken-vol-static
  name: mediakraken-vol-static
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
