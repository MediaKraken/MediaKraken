# deps
apt install libwebkit2gtk-4.1-dev libgtk-3-dev libayatana-appindicator3-dev

# push out x from mkcode for testing desktop
apt install xfce4 xfce4-goodies -y
apt install xrdp -y
cd ~
echo "xfce4-session" | tee .xsession
# did the above for metaman as well - and set rights
systemctl restart xrdp

# remmia on client machine
apt install remmina remmina-plugin-*
