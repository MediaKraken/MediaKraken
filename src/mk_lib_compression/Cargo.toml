[package]
name = "mk_lib_compression"
version = "0.0.2"
edition = "2021"

[profile.release]
panic = 'abort'
strip = "symbols"
lto = true
codegen-units = 1

[dependencies]
flate2 = "1.0.30"
serde_json = "1.0.117"
tar = "0.4.40"
zip = "2.1.2"

[dev-dependencies]
tokio-test = "*"
