[package]
name = "mk_lib_logging"
version = "0.0.31"
edition = "2024"

[profile.release]
panic = 'abort'
strip = "symbols"
lto = true
codegen-units = 1

[dependencies]
chrono = { version = "0.4.40", features = ["serde"] }
reqwest = { version = "0.12.15", default-features = false, features = [
    "http2",
    "json",
    "native-tls",
] }
reqwest-middleware = { version = "0.4.1", features = [
    "http2",
    "json",
] }
reqwest-retry = "0.7.0"
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.140"
stdext = "0.3.3"
tokio = { version = "1.44.1", features = ["full"] }

[dev-dependencies]
tokio-test = "*"
