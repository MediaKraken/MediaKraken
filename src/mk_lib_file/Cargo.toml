[package]
name = "mk_lib_file"
version = "0.0.247"
edition = "2024"
[profile.release]
panic = 'abort'
strip = "symbols"
lto = true
codegen-units = 1

[dependencies]
mk_lib_database = { version = "0.0.269", registry = "kellnr", optional = true }
mk_lib_logging = { version = "0.0.238", registry = "kellnr" }

aws-config = { version = "1.11.0", optional = true }
aws-sdk-s3 = { version = "1.143.0", optional = true }
reqwest = { version = "0.13.4", default-features = false, features = ["json", "native-tls"], optional = true }
serde = { version = "1.0.228", features = ["derive"], optional = true }
serde_json = "1.0.150"
tokio = { version = "1.53.1", features = ["full"] }
uuid = { version = "1.25.0", features = ["serde", "v4"] }
walkdir = "2.5.0"

[features]
s3 = ["dep:aws-config", "dep:aws-sdk-s3"]
garage-admin = ["dep:reqwest", "dep:serde"]

[dev-dependencies]
tokio-test = "*"
