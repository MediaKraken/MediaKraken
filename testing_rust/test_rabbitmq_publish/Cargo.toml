[package]
name = "mkrabbitmqpublish"
version = "0.0.1"
edition = "2021"

[profile.release]
panic = 'abort'
strip = "symbols"
lto = true
codegen-units = 1

[dependencies]
amiquip = { version = "0.4", default-features = false }

[dev-dependencies]
tokio-test = "*"
