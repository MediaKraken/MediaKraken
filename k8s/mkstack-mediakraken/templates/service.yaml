apiVersion: v1
kind: Service
metadata:
  name: {{ include "mkstack-mediakraken.fullname" . }}
  labels:
    {{- include "mkstack-mediakraken.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mkstack-mediakraken.selectorLabels" . | nindent 4 }}
