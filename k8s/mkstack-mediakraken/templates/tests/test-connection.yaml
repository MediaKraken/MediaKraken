apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mkstack-mediakraken.fullname" . }}-test-connection"
  labels:
    {{- include "mkstack-mediakraken.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mkstack-mediakraken.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
