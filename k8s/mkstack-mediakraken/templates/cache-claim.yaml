apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mkstack-cache-claim
  namespace: mkstack-mediakraken
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-csi
