apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mkstack-cache-claim
  namespace: mediakraken
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: longhorn
