apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mkstack-mumble-claim
  namespace: mediakraken
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
  storageClassName: local-path
