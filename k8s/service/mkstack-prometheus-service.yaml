apiVersion: v1
kind: Service
metadata:
  name: mkstack-prometheus-service
  namespace: mediakraken
spec:
  type: NodePort
  ports:
    - name: "9090"
      port: 9090
      targetPort: 9090
      nodePort: 30990
  selector:
    io.kompose.service: mkstack-prometheus-service
